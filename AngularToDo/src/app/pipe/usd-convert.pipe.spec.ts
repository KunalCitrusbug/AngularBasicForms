import { UsdConvertPipe } from './usd-convert.pipe';

describe('UsdConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new UsdConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
